// Moneta frontend logic is currently embedded in index.html so the existing
// single-file frontend remains directly runnable. Backend API integration is
// initialized from index.html and served by FastAPI at /api/*.
